{
  "name": "actions-for-vscode",
  "displayName": "Actions For VSCode",
  "description": "Add custom actions to context menus with configurable commands.",
  "version": "0.4.0",
  "publisher": "your-publisher-name",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/bassmanitram/actions-for-vscode.git"
  },
  "bugs": {
    "url": "https://github.com/bassmanitram/actions-for-vscode/issues"
  },
  "homepage": "https://github.com/bassmanitram/actions-for-vscode#readme",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "actionsForVscode.showExplorerActions",
        "title": "Actions For VSCode..."
      },
      {
        "command": "actionsForVscode.showScmActions",
        "title": "Actions For VSCode..."
      },
      {
        "command": "actionsForVscode.showEditorActions",
        "title": "Actions For VSCode..."
      },
      {
        "command": "actionsForVscode.openSettings",
        "title": "Actions For VSCode: Open Settings"
      },
      {
        "command": "actionsForVscode.manageActions",
        "title": "Actions For VSCode: Manage Actions"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "actionsForVscode.showExplorerActions",
          "group": "2_workspace@1",
          "when": "explorerResourceIsFolder || explorerResourceIsFile"
        }
      ],
      "scm/resourceState/context": [
        {
          "command": "actionsForVscode.showScmActions",
          "group": "navigation"
        }
      ],
      "editor/context": [
        {
          "command": "actionsForVscode.showEditorActions",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Actions For VSCode",
      "properties": {
        "actionsForVscode.useSubmenu": {
          "type": "boolean",
          "default": false,
          "description": "Show actions in a submenu (true) or directly in context menu (false)."
        },
        "actionsForVscode.actions": {
          "type": "array",
          "default": [
            {
              "id": "openOrigin",
              "label": "Open Origin",
              "command": "G",
              "contexts": [
                "explorer",
                "scm"
              ],
              "icon": "link-external",
              "showNotification": true,
              "enabled": true
            }
          ],
          "markdownDescription": "**[📝 Open Visual Settings Manager](command:actionsForVscode.manageActions)** - Manage actions with an easy-to-use interface (recommended)\n\n---\n\nArray of custom actions to add to context menus.\n\n**Available placeholders:**\n- `{path}` - Full path to selected item\n- `{file}` - Name only (no path)\n- `{files}` - All selected files (space-separated, quoted)\n- `{dir}` - Parent directory\n- `{filename}` - Alias for {file}\n- `{workspace}` - Workspace root\n\n**Example:**\n```json\n{\n  \"id\": \"gitLog\",\n  \"label\": \"Git Log\",\n  \"command\": \"git log {path}\",\n  \"cwd\": \"{dir}\",\n  \"contexts\": [\"explorer\"],\n  \"icon\": \"git-commit\"\n}\n```",
          "items": {
            "type": "object",
            "required": [
              "id",
              "label",
              "command"
            ],
            "properties": {
              "id": {
                "type": "string",
                "description": "Unique identifier (alphanumeric, no spaces).",
                "pattern": "^[a-zA-Z0-9_-]+$"
              },
              "label": {
                "type": "string",
                "description": "Text displayed in the context menu."
              },
              "command": {
                "type": "string",
                "description": "Command to execute. Use {path}, {file}, {files}, {dir}, {filename}, or {workspace} as placeholders."
              },
              "cwd": {
                "type": "string",
                "description": "Working directory for command execution. Supports placeholders: {path}, {file}, {dir}, {workspace}. Default: for files = parent dir, for folders = the folder itself."
              },
              "contexts": {
                "type": "array",
                "description": "Where to show this action.",
                "items": {
                  "type": "string",
                  "enum": [
                    "explorer",
                    "scm",
                    "editor"
                  ]
                },
                "default": [
                  "explorer"
                ],
                "uniqueItems": true
              },
              "icon": {
                "type": "string",
                "description": "VS Code icon ID (e.g., 'file', 'folder', 'symbol-class', 'gear'). See: https://code.visualstudio.com/api/references/icons-in-labels"
              },
              "showNotification": {
                "type": "boolean",
                "description": "Show notification when command executes.",
                "default": true
              },
              "enabled": {
                "type": "boolean",
                "description": "Enable or disable this action.",
                "default": true
              }
            }
          }
        },
        "actionsForVscode.commandTimeout": {
          "type": "number",
          "default": 30000,
          "description": "Timeout for command execution in milliseconds.",
          "minimum": 1000,
          "maximum": 300000
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile",
    "package": "vsce package --no-yarn",
    "install-extension": "bash install.sh",
    "reinstall": "npm run compile && npm run package && code --install-extension $(ls -t *.vsix | head -n1)"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/vscode": "^1.80.0",
    "typescript": "^5.0.0",
    "vsce": "^2.11.0"
  }
}
